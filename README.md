 \LATEX has no direct way to achieve a writing on a slant surface drawn on the page. We used SVD computation to get that effect. Then we explored the application of SVD in Face Recognition and finally we wrote a program to find SVD of a matrix from
scratch using QR decomposition.
